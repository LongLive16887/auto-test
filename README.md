# PROMA ADMIN
 
Build with React  node version V20+

## DEPLOYMENT

```
npm install

```
```
npm run build

```

Natija: build/ katalogida statik fayllar (index.html, CSS, JS) paydo bo‘ladi.

```
sudo cp -r build/* /var/www/html/

```
